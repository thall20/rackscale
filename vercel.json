{
  "buildCommand": "pnpm --filter @workspace/rackscale run build",
  "outputDirectory": "artifacts/rackscale/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
